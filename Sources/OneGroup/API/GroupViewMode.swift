import Foundation

/// Group list view mode (`GroupManager.ViewMode`).
public enum GroupViewMode: String, Sendable, Equatable {
    case `default` = "VIEW_MODE_DEFAULT"
    case tree = "VIEW_MODE_TREE"
}
